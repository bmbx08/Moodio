<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0A0716"
    android:padding="16dp">

    <!-- 상단 앱 이름과 설정 아이콘을 위한 가로 레이아웃 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <TextView
            android:id="@+id/appTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Moodie"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textAlignment="center"
            android:paddingLeft="30dp"
            android:textColor="#FFFFFF"
            android:layout_weight="1"/>

        <ImageView
            android:id="@+id/settingsIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_preferences"/>
    </LinearLayout>


    <!-- 첫 번째 버튼 행 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        >
        <!-- 인사말 -->
        <TextView
            android:id="@+id/greeting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="반가워요, 정기찬님!"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="32dp"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="16dp">

            <Button
                android:id="@+id/btnEmotionDiary"
                android:layout_width="0dp"
                android:layout_height="160dp"
                android:layout_weight="1"
                android:layout_margin="8dp"
                android:text="감정 일기"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:backgroundTint="@null"
                android:background="@drawable/home_rounded_button"
                />

            <Button
                android:id="@+id/btnTodoList1"
                android:layout_width="0dp"
                android:layout_height="160dp"
                android:layout_weight="1"
                android:layout_margin="8dp"
                android:text="심리 카드"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:background="@drawable/home_rounded_button"/>
        </LinearLayout>

        <!-- 두 번째 버튼 행 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="32dp">

            <Button
                android:id="@+id/btnTodoList2"
                android:layout_width="0dp"
                android:layout_height="160dp"
                android:layout_weight="1"
                android:layout_margin="8dp"
                android:text="할 일 목록"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:background="@drawable/home_rounded_button"/>

            <Button
                android:id="@+id/btnTest"
                android:layout_width="0dp"
                android:layout_height="160dp"
                android:layout_weight="1"
                android:layout_margin="8dp"
                android:text="불안/스트레스 테스트"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:background="@drawable/home_rounded_button"/>
        </LinearLayout>
    </LinearLayout>

    <!-- 하단 명언 -->
    <TextView
        android:id="@+id/quote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="&quot;If you think your mind is everything, you become that.&quot; - Buddhism"
        android:textColor="#AAAAAA"
        android:textSize="12sp"
        android:gravity="center"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="20dp"/>
</LinearLayout>